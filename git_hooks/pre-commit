#!/usr/bin/env bash
# pre-commit: quick sanity — typecheck changed TS.
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

if git diff --cached --name-only --diff-filter=ACM | grep -qE '\.ts$'; then
  echo "pre-commit: typechecking..."
  npm run typecheck
fi
exit 0
